## Concept Suggestions (Optional)

If you notice a recurring term that appears analytically significant but is not represented in the concept framework, you may add it to `suggested_new_concepts`. Include `term`, `frequency_in_paper`, `reason`, and `closest_existing`.

This is optional — focus primarily on mapping to existing concepts.
