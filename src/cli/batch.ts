/**
 * CLI batch mode entry point.
 *
 * Usage:
 *   npx abyssal-batch [OPTIONS]
 *
 * Options:
 *   --stage {all|discover|acquire|analyze|synthesize|article}
 *   --papers {paper_id_1,paper_id_2,...}
 *   --filter {json_filter}
 *   --config {path}
 *   --dry-run
 */
